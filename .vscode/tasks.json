{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build & run main.nim",
            "type": "shell",
            "command": "nim c -r main.nim",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}